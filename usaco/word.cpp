#include <bits/stdc++.h>
#include <fstream>
using namespace std;

int main(){
	//ifstream fin("word.in");
	//ofstream fout("word.out");
	int w, k;
	string n;
	cin >> w >> j;
	cin >> n;
	vector<int> g(w);
	int point = 0	
	for(int i = 0; i < n.length(); i++){
		if(n[i] != ' ')
			g[point]++;
		else
			point++;
	}		
	int k = 0	
	for(int i = 0; i<g.length(); i++){
		if(g[k] + g[k+1]< j){
			

	return 0;
}
