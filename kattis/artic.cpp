#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
	int t; cin >> t;
	int s, p, x, y;
	vector<pair<int, int>> g;
	while(t--){
		cin >> s >> p;
		for(int i = 0; i < p; i++){
			cin >> x >> y;
			g.push_back({x, y});
		}

		
	}	
	return 0;
}
